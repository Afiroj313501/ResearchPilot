import { ArrowLeft, Loader2, Settings as SettingsIcon } from "lucide-react";
import { useEffect, useState } from "react";
import type { FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { getProfile, updateProfile } from "../lib/user.api";
import { useAuthStore } from "../store/auth.store";
function Settings() { const navigate=useNavigate();const setAuth=useAuthStore(s=>s.setAuth);const token=useAuthStore(s=>s.token);const [name,setName]=useState("");const [email,setEmail]=useState("");const [currentPassword,setCurrentPassword]=useState("");const [newPassword,setNewPassword]=useState("");const [saving,setSaving]=useState(false);const [message,setMessage]=useState("");useEffect(()=>{getProfile().then(r=>{setName(r.data.user.name??"");setEmail(r.data.user.email);}).catch(()=>setMessage("Unable to load your account."));},[]);const save=async(e:FormEvent)=>{e.preventDefault();setSaving(true);setMessage("");try{const r=await updateProfile({name:name.trim(),email:email.trim(),...(newPassword?{currentPassword,newPassword}:{})});if(token)setAuth(token,r.data.user);setCurrentPassword("");setNewPassword("");setMessage("Account updated.");}catch(err:any){setMessage(err?.response?.data?.message||"Unable to update your account.");}finally{setSaving(false);}};return <main className="min-h-screen bg-[#060b16] p-6 text-slate-100"><div className="mx-auto max-w-xl"><button onClick={()=>navigate("/dashboard")} className="mb-7 inline-flex items-center gap-2 text-sm text-slate-400"><ArrowLeft size={16}/> Dashboard</button><div className="mb-7 flex items-center gap-3"><SettingsIcon className="text-cyan-400"/><div><h1 className="text-2xl font-bold">Settings</h1><p className="text-sm text-slate-500">Manage your account information.</p></div></div><form onSubmit={save} className="space-y-5 rounded-2xl border border-white/10 bg-[#080e19] p-6"><label className="block text-sm text-slate-400">Display name<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-slate-100"/></label><label className="block text-sm text-slate-400">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-slate-100"/></label><div className="border-t border-white/10 pt-5"><p className="mb-3 text-sm font-medium text-slate-300">Change password <span className="font-normal text-slate-600">(optional)</span></p><label className="block text-sm text-slate-400">Current password<input type="password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-slate-100"/></label><label className="mt-3 block text-sm text-slate-400">New password<input type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} minLength={8} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-slate-100"/></label></div>{message&&<p className={`text-sm ${message==="Account updated."?"text-emerald-400":"text-red-300"}`}>{message}</p>}<button disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-cyan-400 px-4 py-2.5 font-semibold text-slate-950 disabled:opacity-50">{saving&&<Loader2 className="animate-spin" size={16}/>}Save changes</button></form></div></main>; }
export default Settings;
